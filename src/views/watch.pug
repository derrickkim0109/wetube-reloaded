extends base

block content 
    div 
        p=video.description
        small=video.createdAt
    a(href=`${video.id}/edit`) Edit Video &rarr; 
    br
    a(href=`${video.id}/delete`) Delete Video &rarr; 

    // * relative url : edit <- current page + /edit
    // ex) localhost:4000/profile/edit-profile/password
    // ===> a(href="photo")
    // localhost:4000/profile/edit-profile/photo

    // * absolute url : /edit <- root url + /edit
    // ===> a(href="/photo")
    // localhost:4000/photo